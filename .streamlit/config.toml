[theme]
# ダークテーマ（背景＝黒 / 文字＝ライトシルバー / アクセント＝水色ネオン）
base = "dark"

# ボタンや選択時のメインカラー（水色ネオン）
primaryColor = "#00f3ff"

# 全体の背景色（黒）
backgroundColor = "#000000"

# サイドバーなどのサブ背景色（ほぼ黒のサーフェス）
secondaryBackgroundColor = "#0c0c12"

# 文字色（ライトシルバー）
textColor = "#e6e8ec"
