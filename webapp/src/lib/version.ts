/**
 * App build marker — bump on each meaningful UI deploy.
 *
 * Shown on the EntryGate and in 設定 →「しらべる」 so you can confirm at a
 * glance whether the live site is serving the latest deployment (vs. a cached
 * old build or a stale immutable deployment URL).
 */
export const APP_VERSION = "2026.09.11 · ui-r112 LOCAL MEMORY";
